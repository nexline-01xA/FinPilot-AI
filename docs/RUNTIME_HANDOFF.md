# Runtime handoff

The FinPilot Docker stack is configured for the verified SQLite demo runtime. Backend health is checked before the frontend service starts. On Windows, run `scripts/start-finpilot.ps1` from the repository root to build, start, wait for backend health, and open the UI.
