# Start

Run `powershell -ExecutionPolicy Bypass -File .\scripts\start-finpilot.ps1` from the repository root. It starts the complete local demo and opens http://localhost:3000 after backend health is ready.
