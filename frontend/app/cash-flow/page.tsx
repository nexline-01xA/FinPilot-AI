import {api} from '@/lib/api';import {Card,StatTile} from '@/components/Card';import ForecastChart from '@/components/ForecastChart';export default async function Page(){const f=await api.forecast(30);const money=(x:number|null)=>x===null?'—':`₹${(x/100).toLocaleString('en-IN',{maximumFractionDigits:0})}`;return <div className="p-8 max-w-6xl"><h1 className="text-xl font-semibold">Cash Flow & Forecast</h1><p className="mt-1 mb-6 text-sm text-ink-600">{f.model} · walk-forward benchmark on {f.benchmark.backtest_points} points.</p><div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6"><StatTile label="Naive MAE" value={money(f.benchmark.naive_baseline_mae_paise)}/><StatTile label="Model MAE" value={money(f.benchmark.model_mae_paise)}/><StatTile label="Beats Naive" value={f.benchmark.model_beats_naive?'Yes':'No'}/><StatTile label="Horizon" value={`${f.horizon_days} days`}/></div><Card className="p-4 mb-6"><ForecastChart points={f.points}/></Card><Card className="p-4"><b className="text-sm">Upcoming obligations</b><div className="mt-3 space-y-2">{f.upcoming_obligations.map(o=><div key={o.id} className="flex justify-between border-b border-paper-100 py-2 text-sm"><span>{o.category.replaceAll('_',' ')}</span><span className="tabular">₹{(o.amount_paise/100).toLocaleString('en-IN')}</span><span className="text-ink-600">{o.due_date.slice(0,10)}</span></div>)}</div></Card></div>}
