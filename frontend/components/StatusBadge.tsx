const tone=(v:string)=>{const s=v.toLowerCase();if(['high','failed','rejected','amount_mismatch','missing_settlement','unknown_debit'].some(x=>s.includes(x)))return 'bg-red-50 text-red-700';if(['medium','awaiting','partial','duplicate','unknown_credit'].some(x=>s.includes(x)))return 'bg-amber-50 text-amber-700';if(['matched','succeeded','approved','low'].some(x=>s.includes(x)))return 'bg-emerald-50 text-emerald-700';return 'bg-stone-100 text-stone-600'};
export default function StatusBadge({value}:{value:string}){return <span className={`inline-flex rounded-full px-2 py-1 text-[11px] font-medium ${tone(value)}`}>{value.replaceAll('_',' ')}</span>}
