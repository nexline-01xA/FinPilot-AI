'use client';
import {useEffect,useState} from 'react';import {api} from '@/lib/api';import type {AuditEvent} from '@/types/api';import StatusBadge from './StatusBadge';
export default function AuditTimeline(){const[events,setEvents]=useState<AuditEvent[]>([]);useEffect(()=>{api.audit().then(setEvents)},[]);return <div className="space-y-2">{events.map(e=><div key={e.id} className="rounded-lg border border-paper-200 bg-white p-3"><div className="flex items-center justify-between"><div><span className="text-sm font-medium">{e.operation}</span><span className="ml-2 text-xs text-ink-600">by {e.actor}</span></div>{e.approval_state&&<StatusBadge value={e.approval_state}/>}</div><div className="mt-1 text-xs text-ink-600 tabular">{e.created_at}</div>{e.error&&<div className="mt-2 text-xs text-red-700">{e.error}</div>}</div>)}</div>}
