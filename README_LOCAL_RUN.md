# Local run

Windows: run `powershell -ExecutionPolicy Bypass -File .\scripts\start-finpilot.ps1` from the repository root. It creates `.env` if needed, starts Docker Compose, waits for backend health and opens http://localhost:3000.
